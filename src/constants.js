/**
 * Global constants for the Tile‑Based Level System.
 */
module.exports = {
/** Width/height of a single tile in world pixels. */
TILE_SIZE: 32,

/** Maximum number of tiles that can be rendered using a ParticleContainer before falling back to a regular Container. */
MAX_PARTICLE_TILES: 2000,
};
